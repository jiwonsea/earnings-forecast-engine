# HANDOFF — CODEX 재검증 요청 **rev-5**: T-3 역방향 DCF (NVDA FY2027Q2)

> 작성 2026-08-10 KST · rev-1 `FAIL` → rev-2 `CONDITIONAL PASS` → rev-3 결정성 3건 → rev-4 **`PASS`** → **규약 권고 2건 반영** · 대상 `T3_nvda_2026-08-10.md` **rev-5**
> 채점 분리 규약(§6.1) ① 산술·기계적용 단계. 발화·테제 판정은 요청하지 않는다.
> **본 문서의 모든 수치 블록은 `gen_handoff.py` 가 생성했다.** rev-1의 FAIL 원인이 핸드오프 내 손타이핑이었으므로, 계획의 `모든 표는 스크립트 생성` 규율을 검증 문서에도 적용한다.

---

## §0. rev-4 `PASS` 회신 반영 — 규약 2건 확정 + **결론 1건 변경**

| 권고 | 조치 | 결과 |
|---|---|---|
| **충족률 표준 분모** = 주가(equity) 기준 주 지표, EV 기준은 순부채≠0 시 reconciliation | 전 표에 **[주]/[대조]** 라벨 부여, 규약 절 신설 | 수치 변화 없음 |
| **마진 축** = 절대 Capex 고정이 아니라 **독립 Capex/매출(또는 D&A/매출)** | **마진 축 v2 [PRIMARY] 신설** | **⚠️ 결론이 뒤집혔다 — 아래** |

**v2 정의:** `ebitda_base = revenue × m` 이면서 `da_to_ebitda_override = ov₀ × m₀ / m`. → D&A/매출 `= m × ov₀ × m₀/m = ov₀·m₀` **(m에 불변)**, `capex = D&A × (actual_capex/da_base)` 이므로 **Capex/매출도 불변**. `m = m₀` 에서 override 가 `ov₀` 로 환원돼 **원함수와 정확히 일치**한다.

| 앵커 | m₀ | EV(원본) | EV(v2, m=m₀) | 동치 |
|---|---:|---:|---:|:--:|
| FY26 | 61.6983% | 1,758,804 | 1,758,804 | ✅ EXACT |
| TTM | 65.2938% | 3,350,317 | 3,350,317 | ✅ EXACT |
| FY27E | 66.5985% | 3,601,474 | 3,601,474 | ✅ EXACT |

**Capex/매출 불변 검증 (FY27E 1차연도):**

| m | EBITDA | D&A | Capex | 매출 | Capex/매출 |
|---:|---:|---:|---:|---:|---:|
| 66.60% | 307,508 | 5,074 | 10,620 | 461,734 | **2.3000%** |
| 85.00% | 392,474 | 5,074 | 10,620 | 461,734 | **2.3000%** |
| 99.00% | 457,117 | 5,074 | 10,620 | 461,734 | **2.3000%** |

**⚠️ 결론 변경:** FY27E·현재가에서 마진 축이 **`UNREACHABLE` → 내재 마진 98.61%** 로 도달 가능해졌다. v1(현행 결합 정의)은 m=99%에서도 목표의 99.64% 에 그친다 — **v1의 기계적 결합(EBITDA↑ → D&A↑ → Capex↑)이 마진 레버를 인위적으로 약화시키고 있었다.** 네 권고가 실제로 결론을 바꿨다. → 산출물 헤드라인도 "닿는 축 2개" → **"3개(할인율·성장·마진)"** 로 정정. 단 98.6% 마진은 경제적으로 불가능한 수준이라 **투자 판단 함의는 불변**이다.

---

## §0-A. rev-3 재검증 지적 해소 — **결정성 결함 3건 전부 수용**

| 지적 | 내 재현 | 조치 |
|---|---|---|
| **"bit-identical" 보장이 호스트에서 거짓** — 리포는 LF, Windows 생성은 CRLF | **전면 수용.** 나는 리눅스 샌드박스와 디바이스 VM(둘 다 LF)에서만 재현을 검증했고, **Windows 호스트를 검증 경로에 넣지 않았다.** `open(..., "w", encoding="utf-8")` 은 플랫폼 기본 개행으로 변환하므로 Windows에서 CRLF가 된다 | `newline="\n"` 로 **LF 고정.** 이제 플랫폼 무관하게 동일 바이트 |
| **첫 실행이 UTF-8 콘솔 없이 실패** — cp949가 `—` 를 못 찍음 | **수용.** `w()` 가 매 줄 `print` 했다 — **산출물 생성이 터미널 코드페이지에 의존**했다. 설계 결함이다 | **콘솔 출력 의존 제거.** 기본은 파일만 쓰고, 출력은 `--print` / `T3_VERBOSE=1` 일 때만 (`errors="replace"` best-effort). 종료 시 **sha256 한 줄만 ASCII로** 출력 |
| **듀레이션 통제군이 결정적이지 않음** — 매출 고정 시 EBITDA 마진 발산 | **수용, 그리고 네 지적이 맞다.** 실측: 매출 고정 +10년 마진 **98.6%**, +40년 **319.7%** — 무의미하다 | **통제군 B(ΔNWC=0) 신설**을 권장군으로 채택(매출·마진 경로 불변, 최종 마진 66.6% 유지). C는 마진 발산 수치와 함께 참고용 강등. 결론 주장도 범위 한정 |

**결정성 검증 (샌드박스에서 실행):** 3회 연속 재생성 bit-identical · `LC_ALL=C PYTHONIOENCODING=ascii` 환경에서도 동일 해시 · `--print` 모드에서도 동일 해시 · CRLF 0개.

**추가 확인 — 리포 정책 위반이었다.** `.gitattributes` 4행이 `* text=auto eol=lf` 로 **LF를 강제**하고 있다(주석: "Fixes the CRLF/LF re-encoding noise observed across Windows-host / Linux-sandbox sessions"). 즉 rev-3까지의 코드는 편의 문제가 아니라 **리포 자체 규약을 어기고 있었다.** 반대로 git 체크아웃 경로는 이미 LF이므로, `newline="\n"` 만 고치면 **생성·체크아웃 양쪽이 LF로 일치**한다.

**듀레이션 3통제군 결과 (+40년, 충족률 주가기준):**

| 통제군 | 조작 | +40년 EV | 충족률 | 부작용 |
|---|---|---:|---:|---|
| A 현행 | — | 3,896,555 | **72.70%** | ΔNWC 드래그 포함 · +40년 최종 마진 66.6% |
| **B ΔNWC=0** | — | 3,915,896 | **73.06%** | **없음 (권장)** · +40년 최종 마진 66.6% |
| C 매출 고정 | — | 3,906,763 | **72.89%** | ⚠️ 마진 발산 · +40년 최종 마진 319.7% |
| target | — | 5,382,228 | **100%** | |

통제군 간 폭 **0.36pp** vs 목표까지 부족분 **26.9pp**. → ΔNWC 드래그는 결론을 만들지 않는다. 다만 **"듀레이션이 본질적으로 가장 약한 레버"라는 일반화는 철회**하고, **"이 모델·이 파라미터에서 PV(FCFF)가 EV의 29.6%뿐이라 명시예측 연장만으로는 도달 불가"** 로 한정한다.

---

## §0-B. rev-2 `CONDITIONAL PASS` 조건 해소

| 조건 | 내 재현 | 조치 |
|---|---|---|
| **1. C-6 정의 분리** | 동일 tol=1e-5·gmult 2.042651 에서 **비반올림 복리 경로 648,405** vs **엔진 projection 최종연도 648,407** — 네 지적대로 2 차이. 원인은 `calc_dcf` 의 연간 `round()` | **T-3c를 두 열로 분리.** 추가로 엔진 연도 라벨이 `base_year+5 = 2031` 인 반면 NVDA 회계라벨은 **FY2032** 임도 명시(라벨 1년 어긋남) |
| **2. 허용오차 정책 단일화** | docstring `1e-6` vs 실제 `TOL=1e-05` 불일치 확인. 본문 V2/V3 절에도 동일 문구가 중복돼 있었다(네가 지적한 것보다 1곳 더) | **스크립트 docstring 한 곳을 single source of truth로 확정**(TOL=1e-05 + 근거 + 유효숫자), 본문은 참조만. 중복 문구 제거 |
| **3. §0.4-1 라벨 강등** | 동의. "차별적 정보 없음"은 **확인된 부재**를 뜻하나 실제로는 비교 대상 부재 | **`UNVERIFIABLE` / 비교 불가**로 강등. "컨센과 같다도 다르다도 주장할 수 없다" 명시 + 해소 조건(FY28~32 EBIT·EBITDA estimate를 기준·회계연도 정렬해 확보) 신설 |
| **주의: "듀레이션이 가장 약한 레버"** | **대조 실험을 실행했다.** `revenue_growth_rates` 를 명시해 6년차 이후 매출을 고정하면 +40년 EV 3,896,555 → 3,906,763 (+10,208), 충족률(주가) **72.70% → 72.89%** (**+0.19pp**) | **주장 유지, 근거 교체.** 매출·NWC 동반 연장은 원인이 아니다. 진짜 이유는 **명시예측 FCFF가 EV의 29.6%뿐**(TV/EV 70.4%)이라는 모델 구조다. T-3d 절 신설 |

**포팅 서술 정정 (네 지적 수용):** rev-2 §2의 "tol 인자만 추가한 원본"은 **부정확한 진술이었다.** 정확히는 **dict 기반 재구현**이며, `terminal_ev_ebitda`(Exit Multiple) 분기는 **아예 이식하지 않았다.** 동치가 확인된 범위는 **T-3이 실제로 밟는 경로에 한정**된다 — FY26·TTM·FY27E의 EV, WACC, 그리고 5개 축 솔버. 미실행 분기(`capex_fade_to`·`actual/prior_nwc`·`terminal_ev_ebitda`·`is_financial`·distress premium)는 **동치 미검증**으로 남는다.

---

## §1. rev-1 판정에 대한 회신 — 전부 독립 재현했다

**결론: 지적 전건 수용. 단 2건은 정의 차이(DISPUTED)로 판정하고 양 기준을 병기했다.**

| Codex 지적 | 내 재현 | 내 판정 | 조치 |
|---|---|---|---|
| **P0 FY26 `tax=17.0`** — as-run은 13.0 | 재현 일치. tax=13 → βL 1.453 · Kd 3.31% · WACC 11.17% / tax=17 → βL 1.452 · Kd 3.15% · WACC 11.17%. **DCF EV는 양쪽 모두 1,758,804** | **ACCEPTED** | 오류 소재는 **rev-1 핸드오프 §3 코드블록 손타이핑**. 산출 코드(`t3_reverse_dcf.py:31`)는 처음부터 `tax=13.0`. **T-3 수치 무영향** |
| **C-1 3dp 8.516% 아님** | tol별 해: 1e-4 → **8.515442%** (Codex 값) · 1e-5 → **8.515770%** (내 값) · 1e-6 → 8.515780%. 폭 0.0026pp | **ACCEPTED** | 둘 다 각자 tol에서 옳다. **3번째 소수는 식별되지 않는다** → 전 산출물 **2dp + tol 밴드** 표기로 강등 |
| **C-3 83.4% · C-5 72.4%** | 분모 정의 차이다. TGR 경계 EV 4,487,984 → **주가기준 83.57%** / **EV기준 83.39%**. 듀레이션 경계 EV 3,896,555 → **주가기준 72.70%** / **EV기준 72.40%** | **DISPUTED** | 내 값은 주가기준, 네 값은 EV기준. 차이는 순현금 60,000이 분자에만 들어가기 때문. **rev-2는 양 기준 병기.** 어느 쪽을 표준으로 할지 판정 요청 |
| **C-6 648,399은 1e-5 아님** | 재현 일치. 1e-4 → 648,405 · 1e-5 → **648,405** · 1e-6 → **648,399** | **ACCEPTED** | rev-1 **핸드오프 §4 C-6만** 손타이핑(1e-6 값 전재). **T-3 산출물 본문은 처음부터 648,405** — 재생성 시 bit-identical 확인 |
| **5-2 “로그만 유일 증빙”** | `git show eaf2dfa:profiles/nvda.yaml` 로 FY26 as-run 전량 복원 확인 | **ACCEPTED** | **정정: git + 로그 2중 보존.** 단 `nvda_ttm.yaml`은 아카이브본(dep 3586)과 실행값(dep 3229)이 달라 **TTM 앵커만은 로그 대조가 필수**다 |
| **5-3 열명** | — | **ACCEPTED** | 본표 열명을 **`DCF 괴리`** 로 변경 + `mc.gap_ratio`와의 구분을 표로 명시 |
| **6-1 듀레이션 fractional stub** | 현 결과에서 듀레이션 축은 **3개 앵커·3개 가격 전부 `OUT_OF_BOUNDS`** — 솔버가 소수 해를 반환한 사례가 **0건** | **ACCEPTED (무영향)** | 임의 정의가 결과에 관여하지 않음을 명시. 정수 연도 경계값만 인용 |
| **6-3 / 6-4 / 6-5 / 6-7 / 6-8 / 6-10** | — | **ACCEPTED** | 6-5(V2 분리)는 사용자 결정 요청으로 상신 |
| **6-6 컨센 basis** | — | **ACCEPTED (DISPUTED 유지)** | §0.4-1 판정을 **“매출 축에 한해 구별 불가, OP/EBITDA 축은 미검증”** 으로 한정 |

**✅ C-8 해소 (rev-2 회신):** 내부 불일치 문의에 "C-8은 CONFIRMED, 정정할 내용 없음"으로 답을 받았다. **C-8은 무변경**(모델 TV/EV 70.4% → 내재 78.7%).

---

## §2. rev-1 `UNVERIFIABLE` 해소 — 파일을 리포에 기록했다

rev-1에서 T-3 산출물 4개가 워크스페이스에 없어 포팅 동치성이 검증 불가였다. **rev-2부터 리포에 기록한다** — 아래 해시는 rev-3 시점 값이다.

| 리포 경로 | sha256 |
|---|---|
| `reports/T3_nvda_2026-08-10.md` | `a916a1cd7f853f241d1e7bed26593cdd20cad8ce7fb263b4935bdde01e73ecda` |
| `scripts/t3/bvt_dcf.py` | `c8727462cdbc6032cbf48468f9832a85b920f2df25a7a2a618e068938a82321a` |
| `scripts/t3/t3_reverse_dcf.py` | `c1e326d8493a78e0991d31e63a0832f1a98828471a04b1b2c0b65c46f5479d8e` |
| `scripts/t3/t3_final.py` | `7a7a0e68a0bed3bd6d5f1ac98aeb15d6a26e215c6ddf4a38e9f3961f4149abb0` |
| `scripts/t3/gen_handoff.py` | `257c5b3010ef507dcd61ab549ec32d3280a40886b97fdcf5eafa2f7ecd44ba27` |

재현: `cd scripts/t3 && python3 t3_final.py` → `T3_nvda_2026-08-10.md` 재생성. **bit-identical 이어야 한다.**

⚠️ **`bvt_dcf.py` 는 "tol만 추가한 원본"이 아니다** (rev-2 서술 정정). Pydantic 모델을 dict로 바꾼 **재구현**이며, `terminal_ev_ebitda`(Exit Multiple) 분기는 이식하지 않았다. 의도적 차이는 솔버 3종의 `tol` 인자 추가 하나뿐이고 나머지는 1:1 대응을 목표로 했으나, **동치가 실증된 범위는 T-3이 실제로 밟는 경로**(FY26·TTM·FY27E EV + WACC + 5개 축 솔버)**에 한정된다.**

---

## §3. [P0] 실 BVT 엔진 재현 — **정정본**

```python
# F:\dev\Portfolio\business-valuation-tool 에서 실행
from engine.dcf import calc_dcf
from engine.wacc import calc_wacc
from engine.gap_diagnostics import (solve_implied_wacc, solve_implied_tgr,
                                    solve_implied_growth_multiplier)
from schemas.models import DCFParams, WACCParams

# --- FY26 앵커 = git eaf2dfa:profiles/nvda.yaml (7/10 as-run) ---
w = calc_wacc(WACCParams(rf=3.2, erp=5.5, bu=1.45, de=0.2,
                         tax=13.0, kd_pre=3.8, eq_w=99.8))   # ★ tax=13.0 (rev-1의 17.0은 오기)
p = DCFParams(ebitda_growth_rates=[0.1, 0.085, 0.07, 0.055, 0.04], tax_rate=7.9,
              capex_to_da=1.52, nwc_to_rev_delta=0.05,
              terminal_growth=3.0, actual_capex=6042,
              capex_fade_to=None, da_to_ebitda_override=0.0291)
d = calc_dcf(133230, 2843, 215938, w.wacc, p, 2026)
# 기대: w.bl == 1.453 · w.ke == 11.19 · w.kd_at == 3.31 · w.wacc == 11.17
# 기대: d.ev_dcf == 1_758_804
target = 202.34 * 24_300_000_000 / 1_000_000 + max(435, 0)   # == 4,917,297.0
solve_implied_wacc(target, 133230, 2843, 215938, p)
#   기대 5.97% (2dp) — 원본 로그 5.97%
solve_implied_growth_multiplier(target, 133230, 2843, 215938, w.wacc, p)
#   기대 4.94x (2dp) — 원본 로그 4.94x
```

**FY27E 앵커:**

| 인자 | 값 | 기대 산출 |
|---|---|---|
| WACCParams | rf=4.56 erp=4.6 bu=1.45 de=3.4 tax=17.0 kd_pre=4.8 eq_w=96.7 | bl=1.491 ke=11.42 kd_at=3.98 **wacc=11.17** |
| DCFParams | growth=[0.18, 0.13, 0.09, 0.06, 0.04] tax_rate=16.6 capex_to_da=2.1 nwc=0.05 tg=3.0 actual_capex=9000 da_ov=0.0165 | |
| calc_dcf | (260600, 4300, 391300, 11.17, p, 2026) | **ev_dcf = 3,601,474** |

---

## §4. 주장 전수 — **정밀도 정정본**

기준: FY27E 앵커 · 현재가 **$223.96** · `target_EV = 223.96 × 24,300,000,000 / 1,000,000 + (-60,000) = 5,382,228` · 솔버 tol **1e-05**

| # | 주장 | 값 (유효숫자 준수) | 비고 |
|---|---|---|---|
| C-1 | 내재 WACC | **8.52%** (tol 밴드 8.5154~8.5181%) | 3dp 주장 철회 |
| C-2 | 내재 성장배수 | **2.043x** | 3dp 유효 |
| C-3 | 내재 TGR | **UNREACHABLE** — 경계 5.00%에서 EV 4,487,984 = 주가기준 **83.57%** / EV기준 **83.39%** | 양 기준 병기 |
| C-4 | 내재 EBITDA 마진 | **UNREACHABLE** — m=99%에서 EV 5,362,470 = $223.15 = 주가기준 **99.64%** / EV기준 **99.63%** | |
| C-5 | 내재 듀레이션 | **UNREACHABLE** — +40년에서 EV 3,896,555 = 주가기준 **72.70%** / EV기준 **72.40%** | 정수 연도 경계값 |
| C-6 | 내재 5년 EBITDA CAGR | **20.00%** · FY32E EBITDA **약 648,000 $M** (tol 밴드 648,399~648,405) | 6자리 주장 철회 |
| C-7 | 내재 Ke / βL | **8.67% / 0.894** | rf=4.56% (stale) 조건부. βL=1.0 분기 rf **4.07%** |
| C-8 | TV 비중 | 모델 **70.4%** → 내재 **78.7%** | 정정 요청 사항 불명 — §1 참조 |
| C-9 | 7/10 대비 | iWACC 9.10→8.52% (**-0.58pp**) · gmult 1.767→2.043x (**+15.61%**) | 앵커 불변, 가격만 이동 |
| C-10 | PT $302.83 | iWACC **7.09%** · gmult **2.899x** · CAGR **28.19%** | target_EV 7,298,769 |

**타 앵커 (현재가, 정정 target):**

| 앵커 | target_EV | 모델 EV | 모델 주가 | iWACC | gmult |
|---|---:|---:|---:|---:|---:|
| FY26 | 5,442,663 | 1,758,804 | $72.36 | 5.69% | 5.372x |
| TTM | 5,400,363 | 3,350,317 | $139.60 | 8.15% | 1.704x |
| FY27E | 5,382,228 | 3,601,474 | $150.68 | 8.52% | 2.043x |

---

## §5. rev-3 판정 요청

| 우선순위 | 항목 |
|---|---|
| **P0-1** | §0 조건 3건 + 주의 1건의 해소가 충분한가 (특히 듀레이션 대조 실험 설계의 타당성) |
| **P0-2** | §2 리포 기록 파일로 **포팅 동치성** 재검증 — 미실행 분기 목록이 정확한가 |
| **P1-1** | **충족률 표준 분모 판정** — 주가 기준 vs EV 기준 중 무엇을 규약으로 채택할 것인가 (rev-2 미회신) |
| **P1-2** | 마진 축 대안 정의(절대 capex 고정 / 독립 capex-매출비율) 중 권고안 — 6-3 후속 (rev-2 미회신) |
| **P2-1** | rev-1·rev-2에서 놓친 취약점 |

*본 문서는 투자 자문이 아니며, 모델 방법론 검증 목적의 내부 분석이다.*
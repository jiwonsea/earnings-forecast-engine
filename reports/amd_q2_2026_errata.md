# AMD 2026 Q2 예측 정정·보완 기록

> **작성: 프린트 전 / 커밋: 프린트 후 — ex-ante 등급 낮음**
>
> 채점 앵커는 프린트 전에 커밋된 `reports/amd_q2_2026_forecast_FROZEN.md` @ `ae3203e`다. 이 문서는 공시된 주석이며 앵커가 아니다. AMD 채점에는 아래 정정 수치를 사용하지 않는다.

## PRE-PRINT ERRATA — 2026-08-05 01:35 KST (2026-08-04T16:35Z)

> **발표 전 정정.** 작성 시각 00:58 KST, 정정 시각 01:35 KST, 예상 발표 06:05 KST — **여전히 프린트 전**이므로 소급 수정 금지 규정에 저촉되지 않는다. Codex 6축 회신(2026-08-05, HEAD `3190cd6` 시점) + 본 세션의 독립 재현 결과다.
>
> **예측 수치는 하나도 바뀌지 않았다.** 정정 대상은 ① 프로비넌스 메타데이터 ② 확신도 라벨 1개뿐이다.

### E-1. `git rev-parse HEAD` 정정 — 헤더 값이 낡았다 (Codex §3 지적, 수용)

| | 값 |
|---|---|
| 헤더 기재 | `990b352b13b6ca826fcce1a98124ce8a3f49a385` |
| Codex 리뷰 시점 | `3190cd6` |
| **동결 시점의 실제 HEAD** | **`ae3203e1688ccd90a01c20317f84378640785ca7`** ("feat(freeze): freeze AMD Q2 2026 forecast") |

동결 작업 중 호스트에서 커밋이 진행되어 HEAD가 `990b352` → `ae3203e`로 **9개 커밋** 이동했다. 본 세션은 `990b352` 워킹트리 스냅샷에서 산출했고 그 값을 헤더에 적었다.
**AMD 산출물 5개는 `ae3203e`에 커밋 완료**(profile · test · FROZEN · forecast md/json). `HANDOFF_CODEX_efe_2026aug_amd.md`는 아직 untracked.

### E-2. 그 사이 커밋이 예측을 움직였는가 — **아니다 (bit-identical 재현)**

`990b352`↔`ae3203e` 사이 9개 커밋(generic postmortem 엔진·GEV/TSLA 프로파일·SK하이닉스 스코어러·SPCX 동결 등)을 얹은 **현재 HEAD 트리에서 프로파일을 재실행**한 결과:

```
990b352 트리 forecast JSON sha256: 30539b14d3840ebb3ebd251419da679d754ff2c05468ad39b3139dd24429b869
ae3203e 트리 forecast JSON sha256: 30539b14d3840ebb3ebd251419da679d754ff2c05468ad39b3139dd24429b869
→ BIT-IDENTICAL. 2026Q2 확률가중 매출 11,600.2 / EPS $1.1086 불변.
```

프로파일 sha256도 `c82117da…44ce`로 불변(호스트↔샌드박스 일치). **동결 수치는 유효하다.**

### E-3. 회귀 수치 갱신 (Codex §3 지적, 수용)

| | 헤더 기재 | **정정** |
|---|---|---|
| `pytest -q` | 233 → 244 passed (`990b352` 트리) | **260 → 271 passed, 1 deselected** (`ae3203e` 트리) |
| AMD 전용 | 11 passed | 11 passed (불변) |

증가분 +27은 그 사이 커밋이 들여온 테스트(`test_generic_postmortem` · `test_tsla_postmortem` · `test_sk_hynix_q2_2026_score` · `test_tsla_profile`)이며 **AMD와 무관**하다. "신규 11개만 증가, 기존 0건 변경"이라는 주장 자체는 두 트리 모두에서 성립한다.

### E-4. 9Q sha256 — **"오래된 값"이 아니라 환경별 canonical이다 (Codex §3에 부분 반박)**

Codex는 헤더의 `077ecb10…933c`를 "과거 기준"이라 판정했으나, `START-efe-q2-2026-00-COMMON.md` §3과 DELTA §5-2는 **두 개의 canonical을 명시**한다:

| 환경 | canonical | 본 검증 |
|---|---|---|
| sandbox, CPython ≤ 3.11 | `077ecb10…933c` | **MATCH** (본 세션, CPython 3.11.15 — `990b352`·`ae3203e` 두 트리 모두) |
| host, CPython ≥ 3.12 | `b979d79f…f6e7` | **MATCH** (Codex, 호스트) |

즉 **두 값 모두 옳고 서로 모순되지 않는다.** 헤더는 이미 "sandbox canonical CPython≤3.11"을 명시하고 있었다. 다만 두 환경 결과를 **함께** 적는 편이 오해가 없으므로 이 정정에 병기한다. → **SK하이닉스 9Q 불변식은 양쪽 환경에서 bit-identical 유지 확인.**

### E-5. **확신도 하향: 매출 "중상" → "중"** (Codex §2 지적, 수용 — 유일한 내용 변경)

§(d-2)의 매출 ABOVE 확신도를 **중상 → 중**으로 낮춘다.
**이유**: 근거인 가이던스-비트 분포가 **N=4**에 불과하다. 통계적 추정치가 아니라 보수적 휴리스틱이며, 표본 4개로 "중상"을 주장하는 것은 §(a-4)에서 스스로 경계한 "정밀해 보이려는" 실패에 가깝다.
**바뀌지 않는 것**: 방향(ABOVE), 포인트값($11,650M), 밴드, non-GAAP EPS 확신도(중). **숫자는 하나도 변하지 않는다.**

### E-6. 산출물 개수 정정 (Codex §4 지적, 수용)

§6축 질의 4항의 "신규 파일 4개"는 오기다. **정확히 6개**: 프로파일 1 · 테스트 1 · FROZEN 리포트 1 · 엔진 출력 2 · 핸드오프 1. **기존 파일 0건 수정**이라는 주장은 유효하다(AMD 고유 문자열·워런트 로직이 `engine/`·`schemas/`에 없음을 Codex가 독립 확인).

### E-7. 워런트 필드 설계 수정 (Codex §6 지적, 수용)

HANDOFF §5 P0-2의 제안 필드명 `warrant_dilution_shares`는 **위험하다** — 최대 발행가능주식수(3.2억주)를 희석주식수에 그대로 더하면 안 되기 때문이다. 마일스톤 충족 여부·측정기간·treasury-stock method를 반영한 **해당 분기 가중평균 증분 희석주식수**를 입력해야 한다.
→ **`incremental_diluted_shares`로 개명**하고, "최대치를 그대로 넣지 말 것"을 필드 문서에 못 박는다. 본 문서 §(f) SF3의 "3.2억주 = 19.4%"는 **최대 노출 규모**이지 분기 EPS에 즉시 반영되는 수치가 아니다 — 이 구분을 여기 명시한다.

### E-8. 정정 후에도 유지되는 것

(a)~(f)의 모든 수치 · 밴드 · A/B 사전등록 · Q3 가이던스 예측 · 주가 방향 주장 · SF1~SF5 · §9 채점 계약 — **전부 원문 그대로 유효**하다.

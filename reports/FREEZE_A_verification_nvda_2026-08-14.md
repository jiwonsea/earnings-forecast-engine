# Freeze-A 검증 — NVDA 후보 v2 `variant_2a` 재현 (2026-08-14)

> 계획 rev-3b **§3.1 Freeze-A** (08-13 예정분, T-4 검증 라운드로 1일 지연) · 수행 2026-08-14 KST · 새 저술·입력 변경 **없음** — 동결 원본 무수정.
> **투자 자문 아님 — 모델 방법론 검증 목적의 내부 분석.**

## 판정: **PASS**

후보 v2 는 고정 해시·결정성·산출 수치·fail-closed 경로 전부에서 재현 검증을 통과했다. Freeze-A CANONICAL 은 `variant_2a` 로 확정 유지된다. 관찰 1건(§2, 콘솔 의존 종료코드)은 동결 산출물 자체에 영향이 없어 판정을 바꾸지 않는다.

## §1. 고정 해시 (⑴)

| 파일 | sha256 (재계산) | 대조 |
|---|---|---|
| `reports/nvda_2026q2_freeze_a_candidate_v2.json` (8,390 B) | `60cc8d23cc916444d062a530eb49c0f627b1e10ac8239fd9affe9e06419bfbef` | rev-3b 등재 기대값과 일치 |
| `reports/nvda_2026q2_freeze_a_candidate.json` (v1, 감사 보존) | `65bfb0a3201f73127c1034780e5eb99bc908ee86aa9d6cbadebd6301f8a4b693` | v2 내부 `supersedes.sha256` 와 일치 |
| `scripts/nvda_2026q2_freeze_a_candidate_v2.py` (생성기) | `ca8c1dd528f56f527ce8d85027bbdd2652e5cd7dfb1b845f3b4f701c3311f10c` | 본 검증에서 재실행한 사본과 동일 |

## §2. 결정성 (⑵) — 격리 재현 3회

| 환경 | 실행 | 산출 JSON sha256 | 비고 |
|---|---|---|---|
| 샌드박스 격리 `/tmp/fa_iso1` | ASCII 콘솔 | `60cc8d23…` (동결본과 비트 동일) | 종료코드 1 — 하단 관찰 |
| 샌드박스 격리 `/tmp/fa_iso2` | ASCII 콘솔 | `60cc8d23…` (비트 동일) | 〃 |
| 디바이스 격리 `~/fa_iso` | 기본 콘솔 | `60cc8d23…` (비트 동일, CRLF 0) | 종료코드 0 |

재현 명령: `cp scripts/nvda_2026q2_freeze_a_candidate_v2.py <격리dir>/ && cd <격리dir> && python3 nvda_2026q2_freeze_a_candidate_v2.py` (동결 원본 디렉터리에서 직접 실행 금지 — cwd 에 씀).

⚠️ **관찰 (판정 불변):** 생성기는 JSON 을 **먼저 기록한 뒤** 한국어 요약을 print 하므로, ASCII 콘솔에서는 요약 단계에서 `UnicodeEncodeError` 로 종료코드 1 이 난다 — **산출 JSON 은 그 시점에 이미 완전·비트 동일**(위 표). UTF-8 콘솔에서는 종료코드 0. 이 스크립트는 08-09 작성분으로 바이트 결정성 규약(콘솔 비의존, 08-12 채택) 이전 산출물이며, **동결 원본 무수정 원칙**에 따라 고치지 않는다. 향후 재실행 시 UTF-8 콘솔 사용 권장.

## §3. 산출 수치 (⑶) — `variant_2a` 독립 재계산

산식을 별도로 다시 써서(원 스크립트 `build()` 미호출) 저장값과 대조했다. `OP = rev×GM − opex(8,500)` · `pretax = OP` (below=0, R2-blunt) · `NI = pretax×(1−tax)` · `EPS = NI / 24,490`:

| 시나리오 | rev($M) | OP($M) | OPM | tax | EPS | 재계산 대조 |
|---|---:|---:|---:|---:|---:|:--:|
| bear (p=0.25) | 89,180 | 57,849.92 | 64.869% | 0.18 | 1.9370 | OK |
| base (p=0.50) | 91,000 | 59,659.00 | 65.559% | 0.17 | 2.0219 | OK |
| bull (p=0.25) | 92,820 | 61,486.28 | 66.242% | 0.16 | 2.1090 | OK |
| **가중** | 91,000 | 59,663.55 | — | — | 2.0225 | OK |

부가 항등식: 경상 순이자 비율 (540−102)/81,615 = 0.5367% — 저장값·T-2 ① 앵커와 일치. T-4 §5 정량 채점 훅이 소비하는 3개 값(base OP 59,659 · OPM 65.559% · 가중 OP 59,663.55)은 모두 본 표의 재계산값과 일치한다.

## §4. fail-closed 경로 (⑷)

| 시험 | 결과 |
|---|---|
| 후보 v2 사본 1바이트 변조 → `gen_t2.py` (격리 `/tmp/fa_fc`) | **종료 1 · 산출물 미생성** — `FATAL: candidate v2 hash mismatch (fail-closed)` |
| 동일 격리 디렉터리, 원본 사본(무변조) → `gen_t2.py` | 종료 0 · T-2 산출 `65d8eda5…` **비트 동일** (양성 대조) |
| `gen_t4.py` 동일 게이트 | Codex rev-4 검증에서 1바이트 파괴 시험 PASS 확인 (종료 1·출력 미생성) |
| 원본 저장소 | 본 검증 전 과정에서 **무수정** — 실행은 전부 격리 사본 |

## §5. 검사 목록 (본 생성기가 수행, 실패 시 본 보고서 미생성)

| 검사 | 결과 |
|---|---|
| H-1 후보 v2 파일 해시 = rev-3b 등재 기대값 `60cc8d23…` | PASS |
| H-2 후보 v1 파일 해시 = v2 내부 supersedes 기록 | PASS |
| H-3 후보 v2 LF 전용(CRLF 0)·NUL 없음 | PASS |
| H-4 스키마 = nvda_2026q2_freeze_a_candidate/v2 · CANONICAL 라벨 = variant_2a | PASS |
| N-1 variant_2a 3개 시나리오 전항 재계산 일치 (rev·OP·OPM·pretax·NI·EPS·below=0·tax·p) | PASS |
| N-2 확률가중 재계산 일치 (p 합 = 1.00) | PASS |
| N-3 경상 순이자 비율 = (540−102)/81,615 (T-2 ① 앵커와 동일 원천) | PASS |
| N-4 base OP·OPM = T-4 §5 정량 채점 훅 소비값 (59,659 / 65.559%) | PASS |

---

*본 문서는 투자 자문이 아니며, 모델 방법론 검증 목적의 내부 분석이다.*